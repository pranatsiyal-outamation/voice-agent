name: Deploy to EC2

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: SSH and deploy
        uses: appleboy/ssh-action@v1
        with:
          host: 52.23.6.215
          username: pranat.siyal
          password: ${{ secrets.EC2_VOICE_AGENT_SSH_PASSWORD }}
          script: bash ~/voice-agent/deploy.sh